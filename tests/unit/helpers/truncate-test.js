import { truncate } from 'osf-meetings/helpers/truncate';
import { module, test } from 'qunit';

module('Unit | Helper | truncate');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = truncate([42]);
  assert.ok(result);
});
