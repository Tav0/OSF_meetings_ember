import { safeEmberstyle } from 'osf-meetings/helpers/safe-emberstyle';
import { module, test } from 'qunit';

module('Unit | Helper | safe emberstyle');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = safeEmberstyle([42]);
  assert.ok(result);
});
